const arr = [12, 4, -3, -43, 12, -4, 3];
// вывести только положительные числа

forEach(arr, function (element) {
    if (arr[i]>0) {
        console.log(arr[i]);
        i++
        
    }
    
    // +=  ===  result + element
})
